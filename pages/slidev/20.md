---
clicks: 2
---

<div class="grid grid-cols-3 gap-8 w-full px-10 mt-20">
  <div class="col-item">
  <Card width="250" v-motion
    :initial="{ boxShadow: '0 8px 10px rgba(0, 0, 0, 0.1)' }"
    :click-1="{ boxShadow: '0 4px 6px rgba(0, 0, 0, 0.15)' }"
    title="安装 Node/Pnpm" 
    icon="mdi-code"
    orientation="portrait">

第一步，需要安装 Node.js和Pnpm

在 mac上安装 node.js 可以通过 `brew install node` 来完成

pnpm是nodejs 下的一个包管理器。通过它来安装 slidev。安装 pnpm 的命令是`npm install -g pnpm`


  </Card>
  </div>
  <div class="col-item">
  <Card width="250" v-motion
    :initial="{ boxShadow: '0 4px 6px rgba(0, 0, 0, 0.1)' }"
    :click-1="{ boxShadow: '0 8px 10px rgba(0, 0, 0, 0.15)' }"
    :click-2="{boxShadow: '0 4px 6px rgba(0,0,0,0.1)'}"
    title="创建第一个演示" 
    icon="mdi-movie-roll"
    orientation="portrait">

在命令行下，进入到工作区目录，运行`pnpm create slidev`

接下来会有提示：包括项目名称（可随意，或与演示文稿相关），是否安装和启动（选 yes），选择包管理器（选择 pnpm）。安装成功后，就会打开浏览器，演示欢迎文稿。

  </Card>
  </div>
  <div class="col-item">
<Card class="abs" width="250" v-motion
    :initial="{ boxShadow: '0 4px 6px rgba(0, 0, 0, 0.1)' }"
    :click-2="{boxShadow: '0 8px 10px rgba(0,0,0,0.15)'}"
    title="开始你的创作" 
    icon="mdi-feather"
    orientation="portrait">

现在，打开编辑模式，修改文档。

停止演示，在命令行界面下按`q`退出。再次启动，可以运行：

<div style="text-align: left">

```bash {all}{lines: false}
npx slidev slides.md -t path/theme
```
</div>

  </Card>
  </div>
</div>

<!--

如何构建此页面？

1. 先对 AI 说：生成一个3列一行，等间距，两边留空的布局
2. 然后将每列内容替换成为 Card 组件
-->


<!--

Slidev 需要运行在一个服务器上，从而演示者与观众可以仅通过网页就能各自访问 slidev。Node.js是一个 javascript 语言的服务端运行环境。

在 mac上安装 node.js 可以通过 `brew install node` 来完成。brew 是一个 mac 上的软件管理工具。一些命令行中运行的工具，我们多通过它来完成安装。

brew 又叫 Homebrew，正常安装比较困难。请在豆包输入，我在中国大陆，如何安装 brew。就能得到正确指示。

在 slidev 的官方文档中，提到了npm, pnpm, yarn,bun 等工具。我们使用 pnpm 就可以了。存在多种软件管理系统，这是历史演进的结果。

[click]

接下来，就可以创建第一个演示了。这需要在命令行模式下运行。

[click]

现在，演示文稿的环境就已搭建好。现在，你可通过编辑模式来，从修改欢迎文稿开始，编辑自己的演示，也可以停掉服务器，创建一个空白的 slides.md，再用以下命令打开它。

注意，当我们通过 npm run dev 进入演示时，要求在当前目录下，存在 slides.md 的文件。当我们要创建第二份演示文稿时，怎么办？

[click]

此时就要用这个npx命令。npx命令的第一个参数总是 slidev；第二个参数是要演示的文稿，可以是任何文件名；第三个参数可选，是要使用的主题。如果是官方主题，只需要写主题名，slidev 会自动下载。如果是自己开发的本地主题，则需要加上到主题的路径。

-->
