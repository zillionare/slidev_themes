---
clicks: 5
layout: two-cols
---

<v-switch at="0">
  <template #0-4>

## 代码语法高亮与动画
```python {all|1|3-4|6}{lines: true}
import pandas as pd

df = pd.read_csv('data.csv')
print(df.head())

print(df.describe())
```

请按<span v-mark="{type:'circle', at: 0, color: 'red'}">空格键</span>查看 slidev 显示效果。

这里的线框称为 Rough Markers。用法见 [slidev 文档](https://cn.sli.dev/features/rough-marker)。示例：

```html
<span v-mark="{type:'circle', color: 'red'}">空格键</span>
```

  </template>

  <template #4-6>
  
  ## magic-move
````md magic-move {at: 5}
```python
import pandas as pd

df = pd.read_csv('data.csv')
print(df.head())

print(df.describe())
```

下面的代码将在第一次点击后，转换为如下代码

```python
import pandas as pd

df = pd.read_parquet('data.parquet')
print(df.head())

print(df.describe())
```
````
  
请打开编辑模式，对照源码看
  </template>
</v-switch>

::right::

<v-switch at="0">
  <template #0-4>

  ## 语法解析

1. 请打开编辑模式，对照源码看
2. 以三个"`" + 语言名开始，这里是<span v-mark="{at:0, color: 'red', type:'box'}">```python</span>
3. 后面紧接一对大括号
4. 高亮代码块用 | 分隔成组
5. 每一组可以是一个数字，也可以是区间，比如3-4
6. 也可以是逗号分隔的数字，比如1,2,3
7. all表示一次高亮所有行。多用于开头
8. 其它用法，请参考文档
  
  </template>
  <template #4-6>

  ## 语法解析

  1. 最外层是4个 backtits 符号（`） + md magic-move，和另一组4个 backtits 包含。
  2. 中间是两个以上的代码块。每个代码块由3个 backtits 符号（`） + 语言名 + 3个 backtits 符号（`）包含。
  3. magic-move 就发生成中间的代码块之间。
  </template>
</v-switch>

<!--
这一张我们介绍一下，slidev中一些开箱即用、又很强大的功能，即代码演示功能。这些功能要在 powerpoint 或者 pages 中做成动画，会花很多时间来创建动画和调节。

在观看时，建议打开编辑模式，对着源码看。

[click]

[click]


-->
